// Shared a11y recipe (Toolzy verbatim) — a visible 2px action-blue focus ring,
// applied to every interactive element. Survives forced-colors mode.
export const focusRing =
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-action-blue focus-visible:ring-offset-2";
